% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{accumulate_trials}
\alias{accumulate_trials}
\title{Create cumulative trial datasets by participant}
\usage{
accumulate_trials(data, .id, start = 32, step = 1, .id_keep = NULL)
}
\arguments{
\item{data}{A data frame containing trial-level data.}

\item{.id}{Character string. Name of the participant identifier column.}

\item{start}{Integer. Number of trials to include in the first cumulative
dataset. Default is \code{32}.}

\item{step}{Integer. Increment in the number of trials between consecutive
cumulative datasets. Default is \code{1}.}

\item{.id_keep}{Optional vector of participant identifiers to retain before
creating cumulative datasets. Default is \code{NULL}, in which case all
participants are retained.}
}
\value{
A list of data frames. Each element contains the cumulative data up
to a given trial count for all participants.
}
\description{
Creates a list of cumulative datasets by progressively adding trials within
each participant. For each cumulative trial value, the function keeps the
first \code{m} trials for each participant and combines participants into a single
dataset. If a participant has fewer than \code{m} trials, all available trials for
that participant are retained.
}
\details{
The function assumes that \code{data} contains a trial-ordering variable named
\code{ntrial}. Data are ordered by participant and \code{ntrial} before accumulation.
}
\examples{
dat_cum <- accumulate_trials(data = dat, .id = "id", start = 32, step = 5)

}
