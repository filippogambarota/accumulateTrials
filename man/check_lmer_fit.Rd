% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{check_lmer_fit}
\alias{check_lmer_fit}
\title{Check convergence and singularity of a mixed-effects model}
\usage{
check_lmer_fit(mm, tol = 1e-04)
}
\arguments{
\item{mm}{A fitted mixed-effects model of class \code{merMod}, such as an object
returned by \code{\link[lme4:lmer]{lme4::lmer()}} or \code{\link[lme4:glmer]{lme4::glmer()}}.}

\item{tol}{Numeric. Tolerance used by \code{\link[lme4:isSingular]{lme4::isSingular()}} to assess
singularity. Default is \code{1e-4}.}
}
\value{
A tibble with one row and three columns:
\describe{
\item{converged}{Logical. \code{TRUE} if no convergence messages are present in
\code{mm@optinfo$conv$lme4}.}
\item{singular}{Logical. \code{TRUE} if the model is singular according to
\code{\link[lme4:isSingular]{lme4::isSingular()}}.}
\item{status}{Character. One of \code{"ok"}, \code{"converged_singular"},
\code{"not_converged"}, or \code{"not_converged_singular"}.}
}
}
\description{
Checks whether a fitted \code{lme4} mixed-effects model converged and whether the
fitted model is singular. The function returns a compact diagnostic summary
that can be appended to model-output tables.
}
\details{
The convergence check is based on the \code{lme4} convergence information stored
in \code{mm@optinfo$conv$lme4}. A model is treated as converged when this object is
\code{NULL} or contains no messages after unlisting.

Singularity is evaluated separately with \code{\link[lme4:isSingular]{lme4::isSingular()}}. Therefore, a
model can be classified as converged but singular.
}
\examples{
fit <- lme4::lmer(
  rt ~ congruence + (congruence | id),
  data = dat
)

check_lmer_fit(fit)

}
